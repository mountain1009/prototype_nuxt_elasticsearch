sync:
  app:
    mode: "two-way-resolved"
    alpha: "./"
    beta: "develop:/home/vagrant/app"
    ignore:
      vcs: true
      paths:
        - "/node_modules"
        - "/log"
        - "/tmp"
        - "/vendor/bundle"